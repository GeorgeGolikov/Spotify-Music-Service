#spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto=update

music-service.type=spotify

#spotify properties
spotify.oauth.baseOAuthUrl=https://accounts.spotify.com/authorize?
spotify.oauth.responseType=code
spotify.oauth.clientId=test
spotify.oauth.clientSecret=test
spotify.oauth.redirectUri=test